// The NOT_SELECTED option is for users that pay for the product PRIOR
// to signing up.
export enum AuthTypes {
  PASSWORD = 'password',
  OAUTH_GOOGLE = 'google',
  PASSWORDLESS = 'passwordless',
  NOT_SELECTED = 'not_selected'
}
